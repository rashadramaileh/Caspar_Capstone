@page
@model CASPAR.Pages.Instructors.Modalities.UpsertModel
@{
}
<form method="post" asp-page="Upsert" onsubmit="return ValidateInput()" enctype="multipart/form-data">
    <input asp-for="objModality.InstructorWishlistModalityId" hidden />
    <input asp-for="objModality.InstructorWishListDetailsId" hidden />

    <div class="row">
        <div class="col-10">
            <div class="border p-3 mt-4 row">
                <div class="col-12 pb-2">
                    <h2 class="text-primary">@(Model.objModality.InstructorWishlistModalityId != 0 ? "Update " : "Create ")Modality</h2>
                    <hr />
                    <div class="mb-3 col-6">
                        <label asp-for="objModality.ModalityId">Modality</label>
                        <select id="ddlSemesterStatus" asp-for="objModality.ModalityId" asp-items="@Model.ModalityList" class="form-select">
                            <option disabled selected>--Select Modality--</option>
                        </select>
                    </div>
                    <div class="col-12">
                        @if (Model.objModality.InstructorWishlistModalityId != 0)
                        {
                            <button type="submit" onclick="return ValidateInput()" class="btn btn-primary" style="width:150px">Update</button>
                        }
                        else
                        {
                            <button type="submit" onclick="return ValidateInput()" class="btn btn-primary" style="width:150px">Create</button>
                        }
                        <!--END ELSE -->
                        <a href="/Instructors/Modalities/Index?id=@Model.objModality.InstructorWishListDetailsId" class="btn btn-secondary" style="width:150px">
                            Back to List
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>